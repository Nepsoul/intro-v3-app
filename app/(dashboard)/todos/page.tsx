/*usign (dashboard)=> group routing */
/*static routing: aleady know the name of route */

console.log(process.argv); //only accessible in node/ server
const TodosPage = () => {
  return <div>Todos page</div>;
};
export default TodosPage;
